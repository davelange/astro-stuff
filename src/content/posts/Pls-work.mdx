---
title: Pls work
---

MDX allows you to [use variables, JSX expressions and components within Markdown content](https://docs.astro.build/en/guides/markdown-content/#mdx-only-features) in Astro. If you have existing content authored in MDX, this integration allows you to bring those files to your Astro project.

<Callout text="hello there" />

The astro add command-line tool automates the installation for you. Run one of the following commands in a new terminal window. (If you aren’t sure which package manager you’re using, run the first command.) Then, follow the prompts, and type “y” in the terminal (meaning “yes”) for each one.

<Banner title="Free beer" description="If your package.json has a &#x22;beer&#x22; script like tinacms build && <your-site-build-cmd>, this likely doesn't need to be changed. If your Netlify config is not running a custom build script (e.g astro build), you would have to change this to tinacms build && astro build" image="/aperture-vintage-HF1IO60tQdc-unsplash.jpg" />
